pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Jenkins Pipeline Started Successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Completed Successfully'
            }
        }
    }
}
